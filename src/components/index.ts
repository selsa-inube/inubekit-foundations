import { palette } from "./palette";
import { text } from "./text";
import { icon } from "./icon";
import { fieldset } from "./fieldset";

const inube = {
  palette,
  text,
  icon,
  fieldset,
};

export { inube };
