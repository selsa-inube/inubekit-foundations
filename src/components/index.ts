import { palette } from "./palette";
import { text } from "./text";
import { icon } from "./icon";
import { spinner } from "./spinner";

const inube = {
  palette,
  text,
  icon,
  spinner,
};

export { inube };
